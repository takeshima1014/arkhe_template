<?php 

if ( ! defined( 'ABSPATH' ) ) exit;

/**
 * Arkhe フィルターフック
 */

// add_filter( 'arkhe_breadcrumbs_position',function( $position ) {

// 	if( !is_single() && !is_home() ) return [];

// 	return $position;
// });